Svelte 5 component for rendering QR codes as inline SVG. Supports error correction level, border, and color customization via props. The same encoder is exported as pure, DOM-free `toQrSvg` (SVG string) and `toQrPath` (geometry) functions for server-side use, via the `/qrcodegen` subpath.
